import styled from "styled-components";

export const Logo = styled.h1`
  font-size: 1.5rem;
  font-weight: 700;

  & > span {
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--color-secondary);
  }
`;
